Place your Windows `.ico` file here as:

- `app_icon.ico`

Recommended size set includes: 16, 24, 32, 48, 64, 128, 256 px layers.

This file is referenced by `installer/inkycal_setup.iss` (`SetupIconFile`).
